package com.challenges;

public class Challenge13_Anagram {

    /**
     * CHALLENGE 13: Two-String Anagram
     * <p>
     * Instructions:
     * An anagram is when all the letters in one string exist in another,
     * but the order of letters does not matter.
     * <p>
     * Write a method that accepts two string arguments and returns true if
     * they are anagrams and false if they are not.
     * <p>
     * Examples:
     * - isAnagram("listen", "silent") -> true
     * - isAnagram("triangle", "integral") -> true
     * - isAnagram("abc", "bca") -> true
     * - isAnagram("abc", "ccb") -> false
     *
     * @param str1 The first string.
     * @param str2 The second string.
     * @return true if the strings are anagrams, false otherwise.
     */
    public boolean isAnagram(String str1, String str2) {
        // TODO: Write your logic here.
        return false;
    }
}